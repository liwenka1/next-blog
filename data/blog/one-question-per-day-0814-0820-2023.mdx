---
title: 0814-0820 每日一题
date: '2023-08-14'
tags: ['算法', '每日一题', 'LeetCode']
draft: false
summary: '1.617. 合并二叉树 2.833. 字符串中的查找与替换 3.2682. 找出转圈游戏输家'
layout: PostSimple
bibliography: references-data.bib
---

---

[617. 合并二叉树](https://leetcode.cn/problems/merge-two-binary-trees/description/)

```js
/**
 * Definition for a binary tree node.
 * function TreeNode(val, left, right) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.left = (left===undefined ? null : left)
 *     this.right = (right===undefined ? null : right)
 * }
 */
/**
 * @param {TreeNode} root1
 * @param {TreeNode} root2
 * @return {TreeNode}
 */
var mergeTrees = function (root1, root2) {
  if (root1 == null) return root2
  if (root2 == null) return root1
  root1.val += root2.val
  root1.left = mergeTrees(root1.left, root2.left)
  root1.right = mergeTrees(root1.right, root2.right)
  return root1
}
```

[833. 字符串中的查找与替换](https://leetcode.cn/problems/find-and-replace-in-string/description/)

```js
/**
 * @param {string} s
 * @param {number[]} indices
 * @param {string[]} sources
 * @param {string[]} targets
 * @return {string}
 */
var findReplaceString = function (s, indices, sources, targets) {
  let n = s.length
  let ans = ''
  for (let i = 0; i < n; i++) {
    if (indices.includes(i)) {
      let index = indices.indexOf(i)
      let len = sources[index].length
      let str = s.substring(i, i + len)
      if (str == sources[index]) {
        ans += targets[index]
        i += len - 1
      } else ans += s[i]
    } else ans += s[i]
  }
  return ans
}
```

[2682. 找出转圈游戏输家](https://leetcode.cn/problems/find-the-losers-of-the-circular-game/description/)

```js
/**
 * @param {number} n
 * @param {number} k
 * @return {number[]}
 */
var circularGameLosers = function (n, k) {
  if (n == 1) return []
  const cnt = new Array(n).fill(0)
  for (let i = 1, j = 1; i <= n; i = (i + j * k) % n, j++) {
    cnt[(i - 1 + n) % n]++
    if (cnt[(i - 1 + n) % n] == 2) break
  }
  const ans = []
  for (let i = 1; i < n; i++) {
    if (cnt[i] == 0) ans.push(i + 1)
  }
  return ans
}
```

---
