---
title: 0619-0625 每日一题
date: '2023-06-19'
tags: ['算法', '每日一题', 'LeetCode']
draft: false
summary: '1.1262. 可被三整除的最大和 2.1595. 连通两组点的最小成本 3.LCP 41. 黑白翻转棋 4.面试题 16.19. 水域大小 5.2496. 数组中字符串的最大值 6.1659. 最大化网格幸福感'
layout: PostSimple
bibliography: references-data.bib
---

---

[1262. 可被三整除的最大和](https://leetcode.cn/problems/greatest-sum-divisible-by-three/)

```js
/**
 * @param {number[]} nums
 * @return {number}
 */
var maxSumDivThree = function (nums) {
  const n = nums.length
  const f = new Array(2).fill([]).map(() => new Array(3).fill(-Infinity))
  f[0][0] = 0
  for (let i = 0; i < n; i++) {
    for (let j = 0; j < 3; j++) {
      f[(i + 1) % 2][j] = Math.max(
        f[i % 2][j],
        f[i % 2][(j + nums[i]) % 3] + nums[i]
      )
    }
  }
  return f[n % 2][0]
}
```

[1595. 连通两组点的最小成本](https://leetcode.cn/problems/minimum-cost-to-connect-two-groups-of-points/)

```js

```

[LCP 41. 黑白翻转棋](https://leetcode.cn/problems/fHi6rV/)

```js

```

[面试题 16.19. 水域大小](https://leetcode.cn/problems/pond-sizes-lcci/)

```js

```

[2496. 数组中字符串的最大值](https://leetcode.cn/problems/maximum-value-of-a-string-in-an-array/)

```js
/**
 * @param {string[]} strs
 * @return {number}
 */
var maximumValue = function (strs) {
  let ans = 0
  for (let i = 0; i < strs.length; i++) {
    if (isNaN(strs[i])) {
      ans = Math.max(ans, strs[i].length)
    } else {
      ans = Math.max(ans, Number(strs[i]))
    }
  }
  return ans
}
```

[1659. 最大化网格幸福感](https://leetcode.cn/problems/maximize-grid-happiness/)

```js

```

---
